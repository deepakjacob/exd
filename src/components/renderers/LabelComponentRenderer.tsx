import React from 'react';
import { ComponentDesignDisplayProps } from '../../types';


const LabelComponentRenderer: React.FC<ComponentDesignDisplayProps> = (props: ComponentDesignDisplayProps) => (
  <div>A label component</div>
);

export default LabelComponentRenderer;
